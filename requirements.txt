# WebSec Platform — Python 3.12+ dependencies
# Install: pip install -r requirements.txt

# ── Core ─────────────────────────────────────────────────────────────────────
flask>=3.1.0
waitress>=3.0.1          # Production WSGI server (cross-platform, no C deps)
python-dotenv>=1.0.1     # .env file support

# ── AI Report Generation ─────────────────────────────────────────────────────
anthropic>=0.40.0        # Claude API client

# ── Dev / testing (install with: pip install -e ".[dev]") ────────────────────
# pytest>=8.3
# pytest-flask>=1.3
# ruff>=0.6
# mypy>=1.11
